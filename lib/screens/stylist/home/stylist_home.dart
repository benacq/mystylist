import 'package:flutter/material.dart';

class StylistHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('STYLIST HOME'),
      ),
    );
  }
}
